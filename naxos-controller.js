(function(){
'use strict';
const brand=document.querySelector('.app-brand .brand-word');
if(!brand)return;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let removedArrivalRules=0;
function stabiliseAssistant(){
  const walk=rules=>{for(const rule of Array.from(rules||[])){if(rule.cssRules){walk(rule.cssRules);continue;}const selector=rule.selectorText||'';const animation=`${rule.style?.animation||''} ${rule.style?.animationName||''}`;if(selector.includes('.evia-stage')&&selector.includes('.evia-face')&&/eviaArrive/i.test(animation)){rule.style.removeProperty('animation');rule.style.removeProperty('animation-name');removedArrivalRules++;}}};
  for(const sheet of Array.from(document.styleSheets||[])){try{walk(sheet.cssRules);}catch(_){}}
}
stabiliseAssistant();
const style=document.createElement('style');style.id='naxos-developer-style';style.textContent=`
.app-brand .brand-word{touch-action:manipulation;user-select:none;-webkit-user-select:none}
.naxos-overlay{position:fixed;inset:0;z-index:9999;background:#000;color:#fff;display:none;overflow:auto;padding:max(10px,env(safe-area-inset-top)) 14px max(18px,env(safe-area-inset-bottom));font-family:system-ui,-apple-system,Segoe UI,sans-serif}
.naxos-overlay.open{display:block}body.naxos-open{overflow:hidden!important}
.naxos-shell{width:min(680px,100%);min-height:100%;margin:auto;display:flex;flex-direction:column;gap:14px}.naxos-toolbar{display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid #333;padding-bottom:8px}.naxos-toolbar span{font-size:10px;letter-spacing:.18em;color:#aaa;font-weight:850}.naxos-toolbar button,.naxos-back{width:42px;height:42px;border:1px solid #444;border-radius:50%;background:#050505;color:#fff;font:inherit;font-size:22px}.naxos-id{display:grid;place-items:center;text-align:center;gap:8px}.naxos-face{width:108px;height:108px;border:6px solid #fff;border-radius:50%;background:#000;display:grid;place-items:center}.naxos-eyes{width:66%;height:38%;display:flex;align-items:center;justify-content:space-between;animation:naxosLook .9s ease 1}.naxos-eye{display:block;width:36%;aspect-ratio:1;border:5px solid #fff;border-radius:50%;background:#000}.naxos-id small{font-size:10px;letter-spacing:.18em;color:#aaa;font-weight:800}.naxos-id h2{margin:0;font-size:29px}.naxos-id p{margin:0;max-width:560px;color:#bbb;font-size:13px;line-height:1.45}.naxos-grid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.naxos-card,.naxos-action{border:1px solid #333;border-radius:15px;background:#090909;color:#fff;padding:14px;text-align:left;font:inherit}.naxos-card strong,.naxos-action strong{display:block}.naxos-card small,.naxos-action small{display:block;color:#999;margin-top:5px;line-height:1.35}.naxos-page{display:grid;gap:10px}.naxos-head{display:grid;grid-template-columns:auto 1fr;gap:10px;align-items:center}.naxos-head h3{margin:0}.naxos-head p{grid-column:1/-1;margin:0;color:#aaa;font-size:12px}.naxos-section{border:1px solid #2b2b2b;border-radius:14px;background:#070707;padding:12px}.naxos-section h4{margin:0 0 8px}.naxos-list{display:grid;gap:7px}.naxos-row{border:1px solid #242424;border-radius:10px;background:#0b0b0b;padding:9px}.naxos-row b{font-size:12px}.naxos-row span{display:block;color:#bbb;font-size:12px;line-height:1.4;margin-top:3px}.naxos-row em{display:block;color:#888;font-size:10px;font-style:normal;margin-top:4px}.naxos-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.naxos-metric{border:1px solid #303030;border-radius:12px;padding:10px}.naxos-metric small{display:block;color:#999;font-size:9px;text-transform:uppercase}.naxos-metric strong{display:block;font-size:18px;margin-top:3px}.naxos-check{display:grid;grid-template-columns:14px 1fr;gap:8px;border:1px solid #292929;border-radius:11px;padding:9px}.naxos-check i{width:9px;height:9px;border-radius:50%;background:#777;margin-top:4px}.naxos-check.ok i{background:#fff}.naxos-check.fail i{background:#000;border:2px solid #fff}.naxos-check strong{font-size:12px}.naxos-check span{display:block;color:#aaa;font-size:11px;margin-top:2px}.naxos-status{min-height:20px;color:#ddd;font-size:12px;font-weight:700}.naxos-code{white-space:pre-wrap;word-break:break-word;font:11px/1.5 ui-monospace,monospace;color:#ddd;background:#030303;border:1px solid #242424;border-radius:11px;padding:10px;max-height:42vh;overflow:auto}
@keyframes naxosLook{0%,100%{transform:translateX(0)}30%{transform:translateX(-9%)}62%{transform:translateX(9%)}}
@media(max-width:520px){.naxos-grid{grid-template-columns:1fr}.naxos-metrics{grid-template-columns:repeat(2,1fr)}}@media(prefers-reduced-motion:reduce){.naxos-eyes{animation:none}}
`;document.head.appendChild(style);
const overlay=document.createElement('section');overlay.className='naxos-overlay';overlay.setAttribute('aria-hidden','true');overlay.innerHTML=`<div class="naxos-shell" role="dialog" aria-modal="true"><div class="naxos-toolbar"><span>DEVELOPER MODE</span><button id="naxosClose" type="button" aria-label="Close Naxos">×</button></div><div class="naxos-id"><div class="naxos-face" aria-hidden="true"><div class="naxos-eyes"><span class="naxos-eye"></span><span class="naxos-eye"></span></div></div><small>NAXOS · DEVELOPER ASSISTANT</small><h2>Naxos</h2><p>Independent developer mode for Andros. Inspect course data, mappings and runtime health, then use safe repair actions without changing assessor decisions.</p></div><div id="naxosContent"></div></div>`;document.body.appendChild(overlay);
const content=overlay.querySelector('#naxosContent'),closeButton=overlay.querySelector('#naxosClose');let lastHealth=[];
const course=()=>window.COURSE||{};const metric=(a,b)=>`<div class="naxos-metric"><small>${esc(a)}</small><strong>${esc(b)}</strong></div>`;const row=(a,b,d='')=>`<div class="naxos-row"><b>${esc(a)}</b><span>${esc(b)}</span>${d?`<em>${esc(d)}</em>`:''}</div>`;const head=(t,p)=>`<div class="naxos-head"><button class="naxos-back" data-go="home" type="button">←</button><h3>${esc(t)}</h3><p>${esc(p)}</p></div>`;
function home(){content.innerHTML=`<div class="naxos-grid"><button class="naxos-card" data-go="course"><strong>Course details</strong><small>Show all loaded KSBs and their wording.</small></button><button class="naxos-card" data-go="map"><strong>Portfolio / KSB map</strong><small>Inspect Skill → Knowledge relationships.</small></button><button class="naxos-card" data-go="health"><strong>Run app health check</strong><small>Check runtime, storage and the jump fix.</small></button><button class="naxos-card" data-go="background"><strong>Background information</strong><small>Build, device and internal app information.</small></button><button class="naxos-card" data-go="repair"><strong>Repair tools</strong><small>Reset assistant motion or refresh the app shell safely.</small></button><button class="naxos-card" data-go="diagnostics"><strong>Developer diagnostics</strong><small>View or download a local JSON report.</small></button></div>`;}
function coursePage(){const d=course(),k=d.knowledge||{},s=d.skills||{},b=d.behaviours||{},p=d.pairs||{};content.innerHTML=`<div class="naxos-page">${head('Course details','The exact course data currently loaded into Andros.')}<div class="naxos-metrics">${metric('Knowledge',Object.keys(k).length)}${metric('Skills',Object.keys(s).length)}${metric('Behaviours',Object.keys(b).length)}</div><div class="naxos-section"><h4>Knowledge</h4><div class="naxos-list">${Object.entries(k).map(([a,v])=>row(a,v)).join('')}</div></div><div class="naxos-section"><h4>Skills</h4><div class="naxos-list">${Object.entries(s).map(([a,v])=>row(a,v,(p[a]||[]).length?`Related: ${(p[a]||[]).join(', ')}`:'')).join('')}</div></div><div class="naxos-section"><h4>Behaviours</h4><div class="naxos-list">${Object.entries(b).map(([a,v])=>row(a,v)).join('')}</div></div></div>`;}
function mapPage(){const pairs=course().pairs||{};content.innerHTML=`<div class="naxos-page">${head('Portfolio / KSB map','Relationships currently loaded for the selected Andros course.')}<div class="naxos-section"><h4>Skill → Knowledge relationships</h4><div class="naxos-list">${Object.entries(pairs).map(([a,v])=>row(a,(v||[]).join(' · ')||'No related Knowledge')).join('')||row('—','No mapping data is loaded.')}</div></div></div>`;}
function duplicateIds(){const seen=new Set(),dup=[];document.querySelectorAll('[id]').forEach(el=>{if(seen.has(el.id))dup.push(el.id);seen.add(el.id);});return [...new Set(dup)];}
async function health(){const d=course(),out=[],push=(name,status,detail)=>out.push({name,status,detail});push('Assistant stage stability','ok',removedArrivalRules?`Removed persistent eviaArrive from ${removedArrivalRules} CSS rule(s).`:'No persistent eviaArrive animation remains on the assistant stage.');push('Course data',Object.keys(d.knowledge||{}).length?'ok':'fail',`${Object.keys(d.knowledge||{}).length} K · ${Object.keys(d.skills||{}).length} S · ${Object.keys(d.behaviours||{}).length} B loaded.`);const dup=duplicateIds();push('DOM IDs',dup.length?'fail':'ok',dup.length?`Duplicate IDs: ${dup.join(', ')}`:'No duplicate IDs found.');try{localStorage.setItem('naxos-runtime-test','1');localStorage.removeItem('naxos-runtime-test');push('Local storage','ok','Writable.');}catch(error){push('Local storage','fail',error.message||'Unavailable.');}push('IndexedDB',window.indexedDB?'ok':'fail',window.indexedDB?'Available.':'Unavailable.');push('Andros animation controller',window.EviaAnimations?'ok':'fail',window.EviaAnimations?'Loaded.':'Missing.');try{const registration=await navigator.serviceWorker?.getRegistration?.();push('Offline app shell',navigator.serviceWorker?.controller?'ok':'warn',navigator.serviceWorker?.controller?`Service worker controlling page${registration?.active?.state?` · ${registration.active.state}`:''}.`:'No active controller on this page.');}catch(error){push('Offline app shell','warn',error.message||'Could not inspect.');}lastHealth=out;return out;}
async function healthPage(){content.innerHTML=`<div class="naxos-page">${head('App health check','Developer consistency checks only.')}<div class="naxos-status">Checking…</div></div>`;const results=await health();content.innerHTML=`<div class="naxos-page">${head('App health check','Runtime checks do not replace real-device testing.')}<div class="naxos-metrics">${metric('Passed',results.filter(v=>v.status==='ok').length)}${metric('Warnings',results.filter(v=>v.status==='warn').length)}${metric('Failed',results.filter(v=>v.status==='fail').length)}</div>${results.map(v=>`<div class="naxos-check ${v.status}"><i></i><div><strong>${esc(v.name)}</strong><span>${esc(v.detail)}</span></div></div>`).join('')}<button class="naxos-action" data-go="health"><strong>Run checks again</strong><small>Read the current runtime state again.</small></button></div>`;}
function backgroundPage(){const build=document.querySelector('meta[name="tilos-build"]')?.content||'unknown';content.innerHTML=`<div class="naxos-page">${head('Background information','Developer-facing details hidden from normal assessor use.')}<div class="naxos-section"><div class="naxos-list">${row('App','Andros')}${row('Build',build)}${row('Current course','Bricklayer · ST0095')}${row('Display mode',matchMedia('(display-mode: standalone)').matches?'Installed / standalone':'Browser tab')}${row('Viewport',`${innerWidth} × ${innerHeight}`)}${row('Service worker',navigator.serviceWorker?.controller?'Controlling page':'Not controlling page')}${row('Jump fix',removedArrivalRules?`Applied to ${removedArrivalRules} stage rule(s)`:'No persistent arrival rule detected')}</div></div><div class="naxos-section"><h4>Naxos boundary</h4><p>Naxos is independent from Andros. Developer tools can inspect and repair the interface, but do not mark KSBs achieved or change assessor decisions.</p></div></div>`;}
function resetMotion(){document.querySelectorAll('.evia-face').forEach(face=>{[...face.classList].filter(v=>v.startsWith('anim-')||v.includes('welcome')).forEach(v=>face.classList.remove(v));face.style.removeProperty('transform');face.style.removeProperty('filter');});try{window.EviaAnimations?.setBusy?.(false);window.EviaAnimations?.setMode?.('idle');}catch(_){}stabiliseAssistant();}
function repairPage(message=''){content.innerHTML=`<div class="naxos-page">${head('Repair tools','Safe developer actions. Staff records are not changed.')}<button class="naxos-action" data-fix="motion"><strong>Reset Andros motion</strong><small>Clear stuck expressions and reapply the jump fix.</small></button><button class="naxos-action" data-fix="worker"><strong>Check for app update</strong><small>Ask any registered service worker to update.</small></button><button class="naxos-action" data-fix="reload"><strong>Reload interface</strong><small>Reload Andros without deleting staff records.</small></button><div id="naxosStatus" class="naxos-status">${esc(message)}</div></div>`;}
async function report(){if(!lastHealth.length)await health();return{generatedAt:new Date().toISOString(),app:'Andros',developerAssistant:'Naxos',build:document.querySelector('meta[name="tilos-build"]')?.content||'unknown',course:{reference:'ST0095',knowledge:Object.keys(course().knowledge||{}).length,skills:Object.keys(course().skills||{}).length,behaviours:Object.keys(course().behaviours||{}).length},health:lastHealth,runtime:{userAgent:navigator.userAgent,viewport:{width:innerWidth,height:innerHeight},standalone:matchMedia('(display-mode: standalone)').matches,serviceWorkerControlled:Boolean(navigator.serviceWorker?.controller),arrivalRulesRemoved:removedArrivalRules}};}
async function diagnosticsPage(){const data=await report(),text=JSON.stringify(data,null,2);content.innerHTML=`<div class="naxos-page">${head('Developer diagnostics','Local runtime information only; no learner evidence or media is included.')}<button class="naxos-action" data-report="download"><strong>Download diagnostic report</strong><small>Save the current JSON report locally.</small></button><pre class="naxos-code">${esc(text)}</pre><div id="naxosReportStatus" class="naxos-status"></div></div>`;}
function open(){overlay.classList.add('open');overlay.setAttribute('aria-hidden','false');document.body.classList.add('naxos-open');try{window.EviaAnimations?.setBusy?.(true);}catch(_){}home();closeButton.focus();}
function close(){overlay.classList.remove('open');overlay.setAttribute('aria-hidden','true');document.body.classList.remove('naxos-open');try{window.EviaAnimations?.setBusy?.(false);}catch(_){}}
let taps=0,lastTap=0,resetTimer=0;brand.addEventListener('click',()=>{const now=Date.now();if(now-lastTap>900)taps=0;taps++;lastTap=now;clearTimeout(resetTimer);resetTimer=setTimeout(()=>taps=0,1400);if(taps>=7){taps=0;navigator.vibrate?.(35);open();}});closeButton.addEventListener('click',close);document.addEventListener('keydown',event=>{if(event.key==='Escape'&&overlay.classList.contains('open'))close();});
overlay.addEventListener('click',async event=>{const go=event.target.closest('[data-go]');if(go){const page=go.dataset.go;if(page==='home')home();if(page==='course')coursePage();if(page==='map')mapPage();if(page==='health')await healthPage();if(page==='background')backgroundPage();if(page==='repair')repairPage();if(page==='diagnostics')await diagnosticsPage();return;}const fix=event.target.closest('[data-fix]');if(fix){const status=overlay.querySelector('#naxosStatus');if(fix.dataset.fix==='motion'){resetMotion();status.textContent='Andros motion reset and stage stability reapplied.';}if(fix.dataset.fix==='worker'){try{const regs=await navigator.serviceWorker?.getRegistrations?.()||[];await Promise.all(regs.map(r=>r.update().catch(()=>{})));status.textContent=`Checked ${regs.length} service worker registration(s) for updates.`;}catch(error){status.textContent=error.message||String(error);}}if(fix.dataset.fix==='reload')location.reload();return;}const reportButton=event.target.closest('[data-report]');if(reportButton){const data=JSON.stringify(await report(),null,2),blob=new Blob([data],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`naxos-tilos-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),800);const status=overlay.querySelector('#naxosReportStatus');if(status)status.textContent='Diagnostic report downloaded locally.';}});
window.NaxosDeveloper={open,close,runHealth:health,stabiliseAssistantStage:stabiliseAssistant,version:'1.0'};
})();